import React, { useCallback, useState } from 'react';
import { createNativeStackNavigator } from '@react-navigation/native-stack';
import type { AppLanguage } from '../i18n/createI18n';
import { IncomingRequestScreen, RideInProgressScreen, RideSummaryScreen } from '../screens/CaptainScreens';
import { CaptainDashboardScreen } from '../screens/captain/CaptainDashboardScreen';
import { ToPickupScreen } from '../screens/captain/ToPickupScreen';
import { StartRideScreen } from '../screens/captain/StartRideScreen';
import { TripInProgressScreen } from '../screens/captain/TripInProgressScreen';
import type { TripSummary } from '../screens/captain/TripInProgressScreen';
import { EndRideScreen } from '../screens/captain/EndRideScreen';
import { RateCustomerScreen } from '../screens/captain/RateCustomerScreen';
import type { MockRideRequest } from '../services/mockRideRequestService';
import { SettingsScreen } from '../screens/SettingsScreen';
const Stack = createNativeStackNavigator();
export function CaptainMainStack({ online, onToggle, onLanguageChange, onTripComplete }: { online: boolean; onToggle: () => void; onLanguageChange: (language: AppLanguage) => Promise<void>; onTripComplete: () => void }) { const [request, setRequest] = useState<MockRideRequest | null>(null); const [requestCycle, setRequestCycle] = useState(0); const dismissRequest = useCallback(() => { setRequest(null); setRequestCycle((cycle) => cycle + 1); }, []); return <Stack.Navigator screenOptions={{ headerShown: false, animation: 'slide_from_right', animationDuration: 260 }}><Stack.Screen name="CaptainHome">{({ navigation }) => <CaptainDashboardScreen online={online} onToggle={onToggle} onSettings={() => navigation.navigate('Settings')} onRequest={setRequest} request={request} requestCycle={requestCycle} onReject={dismissRequest} onAccept={() => { const activeRequest = request; dismissRequest(); if (activeRequest) navigation.navigate('ToPickup', { request: activeRequest }); }} />}</Stack.Screen><Stack.Screen name="ToPickup">{({ navigation, route }) => { const activeRequest = (route.params as { request: MockRideRequest }).request; return <ToPickupScreen request={activeRequest} onBack={() => navigation.goBack()} onArrived={() => navigation.navigate('StartRide', { request: activeRequest })} />; }}</Stack.Screen><Stack.Screen name="StartRide">{({ navigation, route }) => { const activeRequest = (route.params as { request: MockRideRequest }).request; return <StartRideScreen onBack={() => navigation.goBack()} onVerified={() => navigation.navigate('TripInProgress', { request: activeRequest })} />; }}</Stack.Screen><Stack.Screen name="TripInProgress">{({ navigation, route }) => <TripInProgressScreen request={(route.params as { request: MockRideRequest }).request} onBack={() => navigation.goBack()} onEndRide={(summary: TripSummary) => navigation.navigate('EndRide', { summary })} />}</Stack.Screen><Stack.Screen name="EndRide">{({ navigation, route }) => <EndRideScreen summary={(route.params as { summary: TripSummary }).summary} onBack={() => navigation.goBack()} onConfirmed={() => navigation.navigate('RateCustomer')} />}</Stack.Screen><Stack.Screen name="RateCustomer">{({ navigation }) => <RateCustomerScreen onDone={() => { onTripComplete(); navigation.popToTop(); }} />}</Stack.Screen><Stack.Screen name="IncomingRequest">{({ navigation }) => <IncomingRequestScreen onBack={() => navigation.goBack()} onDecline={() => navigation.goBack()} onAccept={() => navigation.replace('RideInProgress')} />}</Stack.Screen><Stack.Screen name="RideInProgress">{({ navigation }) => <RideInProgressScreen onBack={() => navigation.navigate('CaptainHome')} onComplete={() => navigation.replace('RideSummary')} />}</Stack.Screen><Stack.Screen name="RideSummary">{({ navigation }) => <RideSummaryScreen onHome={() => navigation.navigate('CaptainHome')} />}</Stack.Screen><Stack.Screen name="Settings">{({ navigation }) => <SettingsScreen onBack={() => navigation.goBack()} onLanguageChange={(language) => { void onLanguageChange(language).then(() => navigation.goBack()); }} />}</Stack.Screen></Stack.Navigator>; }
